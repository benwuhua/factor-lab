# Autoresearch Reports

Generated autoresearch ledgers and run artifacts live here locally.

Tracked:

- This README only.

Ignored:

- `expression_results.tsv`
- `pattern_results.tsv`
- `combo_results.tsv`
- `runs/`

Use generated summaries as research evidence, but commit only source specs, docs, and small reference artifacts that should be shared.
